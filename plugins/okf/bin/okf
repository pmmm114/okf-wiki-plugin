#!/usr/bin/env bash
# okf 셔틀 (T-P5-4) — 동봉 엔진(core → okf-core 심링크)을 uv로 실행한다.
# 네트워크 설치 스텝 없음: 플러그인 업데이트가 곧 엔진 업데이트다(D2 직배달).
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec uv run --project "$here/../core" okf "$@"
