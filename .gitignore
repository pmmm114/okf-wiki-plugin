__pycache__/
.venv/
dist/
uv.lock
oracle-diff-report.md

# 로컬 전용 — repo의 일부가 아니다(문서 링크 게이트 스코프도 git 추적 기준).
.claude/settings.local.json
.claude/worktrees/
.remember-plugin/

# lefthook이 생성하는 훅 셸(설치한 머신의 경로가 박히므로 커밋하지 않는다).
# `python3 scripts/install_hooks.py`가 다시 만든다.
.githooks/
